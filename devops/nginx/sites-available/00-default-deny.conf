# 未知 HTTP Host
server {
    listen 80 default_server;
    server_name _;

    return 444;
}


# 未知 HTTPS Host
server {
    listen 443 ssl default_server;
    server_name _;

    ssl_reject_handshake on;
}
