upstream backend_pool {

    server backend1.local
        max_fails=3
        fail_timeout=30s;

    server backend2.local
        max_fails=3
        fail_timeout=30s;

    # 示例：
    # server backend1.local weight=3;
}
